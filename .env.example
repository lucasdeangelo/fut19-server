DATABASE_URL="postgresql://fut:fut@127.0.0.1:5433/fut19?schema=public"
JWT_SECRET="8271348971230947209837418273"
HOST="0.0.0.0"
PORT="3000"
CORS_ORIGIN="http://localhost:3005"

# Captura de tráfego de observação (pesquisa do protocolo FIFA 19).
# Quando habilitada, requests/responses sob /fifa são registrados em CAPTURE_DIR
# com redação de campos sensíveis (senhas, tokens, cookies).
CAPTURE_ENABLED=false
CAPTURE_DIR=./captures